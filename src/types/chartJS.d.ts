export type LineChartDataset = {
  label: string;
  data: number[];
  borderColor: string;
  backgroundColor: string;
  borderWidth: number;
};

export type BarChartDataset = {
  label: string;
  data: number[];
  borderColor: string;
  backgroundColor: string;
};
